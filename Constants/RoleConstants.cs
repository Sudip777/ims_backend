using inventory_management_system.Enums;

namespace inventory_management_system.Constants
{
       public static class RoleConstants
        {
            public const string ADMIN = nameof(UserRole.ADMIN);
            public const string MANAGER = nameof(UserRole.MANAGER);
            public const string SALES = nameof(UserRole.SALES);
            public const string WAREHOUSE = nameof(UserRole.WAREHOUSE);
            public const string SUPPORT = nameof(UserRole.SUPPORT);
            public const string SUPER_ADMIN = nameof(UserRole.SUPER_ADMIN);
        }
    
}
